# ripwire field notes v1 — one per line: <canonical-id or path> <TAB> <ISO-date> <TAB> <text> [<TAB> <HEAD sha> <TAB> <branch>]. Kept SORTED (merge-friendly union); dates are git committer-clock, not wall time; the trailing sha/branch pair is present only on provenance-stamped notes.
crates/hwp-render/src/layout.rs	2026-09-18	PARA_LINE_SEG flags 0x60000 are NOT a synthesized-cache marker; genuine 5.0.2.x single-page files (fixtures/hwp5/work_report.hwp) carry it on every line, so bit0/bit1 absence never proves the cache is ours	1dc42602b8faf3b394a69de1c3498d044bc480d7	docs/ripwire-adoption
crates/hwp-render/src/layout.rs	2026-09-18	a table row's stored cell.height is only a lower bound; Hancom HWPX export writes the declared minimum, and hwp edit keeps the pre-edit height under a re-synthesized cache (#245/#233); cell_cache_total is the floor	1dc42602b8faf3b394a69de1c3498d044bc480d7	docs/ripwire-adoption
crates/hwp-render/src/lineseg.rs	2026-09-18	synthesize_linesegs recomputes body paragraphs with page-relative v_pos resets; only fill_list (cells, captions, text boxes) honors force=false and skips paragraphs that still have segments	1dc42602b8faf3b394a69de1c3498d044bc480d7	docs/ripwire-adoption
fixtures/samples/report-tables.hwpx	2026-09-18	pdftotext drops text from overlapping or one-line-sliver cell fragments; verify render coverage from the DisplayList (Item::Glyphs per page), not from the PDF text layer	1dc42602b8faf3b394a69de1c3498d044bc480d7	docs/ripwire-adoption
